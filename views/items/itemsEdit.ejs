<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Item</title>
</head>
<body>
    <a href="/"><h1>Fasion Store</h1></a>
    <h1>Edit Item</h1>
    <main>
        <form action="/items/<%= selectedItem._id %>?_method=PUT" method="POST">
            <div>
                <img src="<%= selectedItem.imgUrl %>" alt="" style="width:400px; height:450px">
            </div>
            <div>
                <label for="item-image-url">Item Image Url</label>
                <input type="text" id="item-image-url" name="imgUrl" required value="<%= selectedItem.imgUrl %>">
            </div>
            <div>
                <label for="item-name">Item Name</label>
                <input type="text" id="item-name" name="name" required value="<%= selectedItem.name %>">
            </div>
            <div>
                <label for="item-description">Description</label>
                <textarea id="item-description" name="description" rows="4" cols="50"><%= selectedItem.description %></textarea>
            </div>
            <div>
                <label for="item-price">Price</label>
                <input type="text" id="item-price" name="price" required value="<%= selectedItem.price %>">
            </div>
            <button type="submit">Submit</button>
        </form>
        <a href="/items/<%= selectedItem._id %>">
            <button>Cancel</button>
        </a>
    </main>
</body>
</html>