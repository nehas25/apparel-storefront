<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/itemsShow.css">
    <title>Fasion Store</title>
</head>
<body>
    <%- include('partials/header') %>
    <a href="/items/<%= foundItem._id %>/edit">Edit</a>
    <form action="/items/<%= foundItem._id %>?_method=DELETE" method="POST">
        <button type="submit">Delete</button>
    </form>

    <div class="itemInfo">
        <div>
            <img src="<%= foundItem.imgUrl %>" alt="" style="width:300px; height:350px">
        </div>
        <section>
            <p><%= foundItem.name %></p>
            <p><%= foundItem.description %></p>            
            <p><%= foundItem.price %></p>        
        </section>
    </div>
</body>
</html>